# ═══════════════════════════════════════════════════════════════════════════════
# ESCROWBASE ENVIRONMENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════
#
# Copy this file to .env.local and fill in your values
#
# ENVIRONMENT MODES:
# - development: Uses testnets and sandbox APIs (no real money)
# - production: Uses mainnets and live APIs (real money!)
#
# ═══════════════════════════════════════════════════════════════════════════════

# ───────────────────────────────────────────────────────────────────────────────
# ENVIRONMENT MODE
# ───────────────────────────────────────────────────────────────────────────────
NODE_ENV=development

# ───────────────────────────────────────────────────────────────────────────────
# DATABASE
# ───────────────────────────────────────────────────────────────────────────────
# Option 1: Local Docker PostgreSQL
# docker run --name escrow-postgres -e POSTGRES_PASSWORD=postgres -p 5432:5432 -d postgres
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/escrowbase?schema=public"

# Option 2: Free cloud database (Supabase, Neon, Railway)
# DATABASE_URL="postgresql://user:password@host:5432/database"

# ───────────────────────────────────────────────────────────────────────────────
# BLOCKCHAIN - BASE SEPOLIA TESTNET
# ───────────────────────────────────────────────────────────────────────────────
NEXT_PUBLIC_CHAIN_ID=84532
NEXT_PUBLIC_CHAIN_NAME="Base Sepolia"
NEXT_PUBLIC_RPC_URL="https://sepolia.base.org"
NEXT_PUBLIC_BLOCK_EXPLORER="https://sepolia.basescan.org"

# Contract addresses (deploy your own or use provided test contracts)
NEXT_PUBLIC_ESCROW_FACTORY_ADDRESS=""
NEXT_PUBLIC_MOCK_USDC_ADDRESS=""
NEXT_PUBLIC_MOCK_USDM_ADDRESS=""

# ───────────────────────────────────────────────────────────────────────────────
# BRIDGE.XYZ CONFIGURATION
# ───────────────────────────────────────────────────────────────────────────────
# Set to "true" to use mock Bridge service (no real API needed)
# When false/missing, real Bridge credentials are required
BRIDGE_USE_MOCK="true"

# Real Bridge credentials (only needed when BRIDGE_USE_MOCK is not "true")
# Get sandbox credentials at: https://dashboard.bridge.xyz
BRIDGE_API_URL="https://api.sandbox.bridge.xyz"
BRIDGE_API_KEY=""
BRIDGE_API_SECRET=""
BRIDGE_WEBHOOK_SECRET=""

# ───────────────────────────────────────────────────────────────────────────────
# SERVER WALLET
# ───────────────────────────────────────────────────────────────────────────────
# Generate a new wallet for testing. Fund with Sepolia ETH from faucet.
# ⚠️ NEVER commit real private keys!
SERVER_PRIVATE_KEY="0x..."
