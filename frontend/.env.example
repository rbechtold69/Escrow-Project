# ═══════════════════════════════════════════════════════════════════════════════
# ESCROWPAYI ENVIRONMENT CONFIGURATION
# ═══════════════════════════════════════════════════════════════════════════════
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env.development for local development
# 2. Copy this file to .env.production for production deployment
# 3. Fill in your actual values (NEVER commit files with real secrets!)
#
# ENVIRONMENT FILES:
# - .env.example        → Template (safe to commit)
# - .env.development    → Local dev with testnets (NEVER commit)
# - .env.production     → Production with mainnet (NEVER commit)
# - .env.local          → Overrides for local machine (NEVER commit)
#
# ═══════════════════════════════════════════════════════════════════════════════

NODE_ENV=development

# ───────────────────────────────────────────────────────────────────────────────
# DATABASE
# ───────────────────────────────────────────────────────────────────────────────
# PostgreSQL connection string (Supabase, Neon, or local Docker)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL=""

# ───────────────────────────────────────────────────────────────────────────────
# BLOCKCHAIN CONFIGURATION
# ───────────────────────────────────────────────────────────────────────────────
# Development: Base Sepolia (testnet)
#   NEXT_PUBLIC_CHAIN_ID=84532
#   NEXT_PUBLIC_RPC_URL="https://sepolia.base.org"
#   NEXT_PUBLIC_BLOCK_EXPLORER="https://sepolia.basescan.org"
#
# Production: Base Mainnet (REAL MONEY!)
#   NEXT_PUBLIC_CHAIN_ID=8453
#   NEXT_PUBLIC_RPC_URL="https://mainnet.base.org"
#   NEXT_PUBLIC_BLOCK_EXPLORER="https://basescan.org"

NEXT_PUBLIC_CHAIN_ID=84532
NEXT_PUBLIC_CHAIN_NAME="Base Sepolia"
NEXT_PUBLIC_RPC_URL="https://sepolia.base.org"
NEXT_PUBLIC_BLOCK_EXPLORER="https://sepolia.basescan.org"

# Smart contract addresses (deploy first, then fill these in)
NEXT_PUBLIC_ESCROW_FACTORY_ADDRESS=""
NEXT_PUBLIC_MOCK_USDC_ADDRESS=""
NEXT_PUBLIC_MOCK_USDM_ADDRESS=""

# ───────────────────────────────────────────────────────────────────────────────
# BRIDGE.XYZ CONFIGURATION
# ───────────────────────────────────────────────────────────────────────────────
# Mock mode: Set to "true" to use mock Bridge (no API needed)
# Real mode: Set to "false" and provide credentials below
BRIDGE_USE_MOCK="true"

# Bridge API Credentials
# Development: https://api.sandbox.bridge.xyz
# Production:  https://api.bridge.xyz
BRIDGE_API_URL="https://api.sandbox.bridge.xyz"
BRIDGE_API_KEY=""
BRIDGE_API_SECRET=""
BRIDGE_CUSTOMER_ID=""
BRIDGE_WEBHOOK_SECRET=""

# ───────────────────────────────────────────────────────────────────────────────
# COINBASE SMART WALLET
# ───────────────────────────────────────────────────────────────────────────────
# No API keys required for basic Smart Wallet functionality.
# The wallet auto-detects testnet/mainnet based on NEXT_PUBLIC_CHAIN_ID.

# ───────────────────────────────────────────────────────────────────────────────
# SERVER WALLET (for contract deployments)
# ───────────────────────────────────────────────────────────────────────────────
# Private key for server-side transactions (deploying contracts, etc.)
# Development: Fund with Sepolia ETH from faucet
# Production: Fund with real ETH for gas
# ⚠️ NEVER commit a real private key!
SERVER_PRIVATE_KEY=""
